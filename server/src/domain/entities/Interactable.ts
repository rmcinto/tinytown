export interface InteractableBase {
    interactableId: string;     // Unique identifier for interactable object.
    interactableType: string;   // Type of interactable object (e.g., "Chest", "Sign").
    name: string;               // Name of the interactable object.
    description: string;        // Description of the interactable object.
}


